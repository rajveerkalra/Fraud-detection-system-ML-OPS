"""
Flink streaming features job (scaffold).

We’ll implement this in Step 2 using Flink (local via Docker; AWS via Managed Flink).
"""


def main() -> None:
    raise SystemExit(
        "Step 2 not implemented yet. Next: add Flink runtime + Kafka connector and implement windowed features."
    )


if __name__ == "__main__":
    main()

